services:
  - type: web
    name: phase10-multiplayer-server
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx tsx server/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
